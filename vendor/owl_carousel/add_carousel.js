jQuery(document).ready(function() { 
  jQuery("#owl-demo").owlCarousel({      
      slideSpeed : 600,
      paginationSpeed : 800,
      autoPlay:5000,
      singleItem:true
  }); 
});

// navigation : true, // Show next and prev buttons